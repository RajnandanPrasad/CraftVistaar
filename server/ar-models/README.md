# AR Models

Place `.glb` model files in this directory to make them available via `/models/<filename>`. For example, a model saved as `chair.glb` will be served at:

`http://localhost:5000/models/chair.glb`

Use the `model3D` field on products to store the file path or filename, for example: `chair.glb` or `/models/chair.glb`.
